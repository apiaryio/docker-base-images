machine:
  services:
    - docker
dependencies:
  pre:
    - ./scripts/install-dependencies.sh
  override:
    - ./scripts/docker-build-images.sh
test:
  override:
    - ./test.sh
deployment:
    production:
        branch: master
        commands:
          - ./scripts/docker-internal-release.sh
